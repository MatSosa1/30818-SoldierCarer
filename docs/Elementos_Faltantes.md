# Elementos Faltantes / Placeholders — SoldierCarer

**Proyecto:** SoldierCarer · Grupo 6
**Documento:** Registro vivo de assets/elementos faltantes y sus placeholders
**Versión:** 1.0 (inicial)

> **Documento vivo.** Claude Code lo **actualiza al cierre de cada sprint** (y al introducir cualquier placeholder nuevo), según la política de `Contexto.md §6-§7`. Las entradas resueltas se marcan como `Resuelto`, no se borran (trazabilidad).
>
> Estados: `Pendiente` · `En producción` · `Resuelto`.
>
> **Convención de estados:** `En producción` = el modelo/asset ya existe en `assets/` (`.blend`, textura…) **pero aún no está integrado** en su escena de juego con el swap del placeholder. `Resuelto` = integrado y reemplazando al placeholder en la escena correspondiente.

---

## 1. Registro

> **Nota de reconciliación (2026-07-18):** el modelado va más avanzado que este registro. Los ítems marcados `En producción` **ya tienen `.blend` en `assets/`** pero aún **no están integrados** en su escena de juego (sigue vivo el placeholder). Al integrarlos (swap del placeholder) pasan a `Resuelto`.

| ID | Elemento | Tipo | Dónde se usa | Placeholder actual | Qué se necesita para reemplazar | Sprint | Responsable | Estado |
|---|---|---|---|---|---|---|---|---|
| PH-001 | Manos + uniforme del jugador VR | Modelo 3D + animación | Rig del jugador (`views/Jugador.tscn`) | **[S1]** `views/Jugador.tscn` ya formalizado como rig `XROrigin3D`/`XRController3D` con manos placeholder (`PH_Mano` + `PH_Manga`, `BoxMesh` planos, script `manos_vr.gd` tiñe según `grip`) en los anclajes finales | Existe `assets/3D/entities/medic/Medic.blend` + brazos (`MilitaryLeftArm.png`/`MilitaryRightArm.png`). Falta: verificar si sirve como mano/manga en primera persona o requiere rig dedicado; animaciones de gesto (vendaje, sutura, disparo); swap de `PH_Mano`/`PH_Manga` sin tocar `manos_vr.gd` | S1 | — | En producción |
| PH-002 | Modelo de soldado herido | Modelo 3D + audio | `SoldadoHerido.tscn` | `PH_` cápsula de Godot | Modelo base de soldado disponible (`entities/model.blend`). Falta: variante con heridas/vendajes, posturas de dolor, set de voz (gemidos, ayuda, confirmación) e integración | S4 | — | En producción |
| PH-003 | Modelo Opositor con pistola | Modelo 3D + animación | `views/EnemigoArma.tscn` | Cápsula naranja + `Label3D` | Existen `entities/enemies/Enemy.blend` + `weapons/pistol/pistol.blend`. Falta: animación de disparo e integración (swap de la cápsula) | S5 | — | En producción |
| PH-004 | Modelo Opositor con navaja | Modelo 3D + animación | `views/EnemigoCuchillo.tscn` | Cápsula roja + `Label3D` | Existen `entities/enemies/Enemy.blend` + `weapons/knife/knife.blend`. Falta: animación de embestida e integración | S5 | — | En producción |
| PH-005 | Ítems del kit médico | Modelos 3D | `views/KitMedico.tscn`, instanciado en `Jugador.tscn` (anclado a la cadera, "mochila") | **[S3]** Mecánica completa y funcional (RF-17..RF-24): 5 `ItemMedico` (`vendas.gd` circular, `alcohol.gd` inclinación, `suturas.gd` presión ×3, `morfina.gd`/`analgesicos.gd` proximidad+gatillo) con `BoxMesh` + `Label3D` como icono; `SecuenciaTratamiento` valida el orden (vendas→alcohol→suturas) y feedback OK/FALLO (`Label3D` + `AudioStreamPlayer3D` sin stream) | Existen `bandage`, `alcohol`, `needle`, `pill`/`tablet`, `skin_stapler`, `kit` en `assets/3D/`. Falta: swap de cada `Icono` (`BoxMesh`) por su modelo real sin tocar los scripts de gesto | S3 | — | En producción |
| PH-006 | Pistola del jugador | Modelo 3D | `views/Jugador.tscn` → `XROrigin3D/ManoDerecha/Pistola/PH_Pistola` | **[S5]** Mecánica completa y funcional (RF-25..RF-28): `BoxMesh` negro simple visible solo con la pistola desenfundada; extracción por proximidad de la mano derecha a `PuntoFunda` (cadera, gesto físico), 7 balas/cargador × 3 cargadores, recarga con botón X/A de la mano izquierda, `Label3D` de cargador (RF-27) | Existe `assets/3D/weapons/pistol/pistol.blend`. Falta: swap de `PH_Pistola` por el modelo real + funda visible en la cadera, sin tocar `pistola.gd` | S5 | — | En producción |
| PH-007 | Escenario E1 (Calle) | Escenario + props | `views/Mision.tscn` → `Escenario_E1_Calle/Ambientacion` | **[S7]** Ambientación funcional con primitivas sin colisión, fuera de los 4 carriles de spawn: 2 vehículos volcados, 2 bloques de "edificio lateral" (límite visual, paleta `#5A5A5A`), 1 grafiti (plano de color liso), 3 árboles (tronco+copa), 4 escombros dispersos | Existe `assets/Escenarios/Scene1/Scene1.blend` + props (`destroyed_car`, `door`, `sofa`, `tree`, `glass_shards`). Falta: swap de cada primitiva por su modelo real; textura de grafiti real (no existe ninguna todavía, ni siquiera placeholder de imagen) | S7 | — | En producción |
| PH-008 | Escenario E2 (Edificio) | Escenario + props | `views/Mision.tscn` → `Escenario_E2_Edificio` | **[S7]** Interior cerrado sin colisión: 4 paredes + techo (bloquean la luz direccional exterior por sombra → baja visibilidad, UI_ART_Design.md §6.2), sofá y mesa volcados, puerta entreabierta + puerta caída, escalera decorativa (4 escalones, no accesible), 3 escombros internos, y un **segundo `SoldadoHerido`** (`escenario="E2_Edificio"`) — antes vacío desde S2 | Existen `sofa`, `table`, `door`, `glass_shards` en `assets/3D/`. Falta: swap de cada primitiva por su modelo real; papeleo/vidrios rotos como textura de piso | S7 | — | En producción |
| PH-009 | Focos de emergencia | Modelo + luz | `views/Mision.tscn` → `Escenario_E1_Calle/Herido/Foco` | **[S4]** `Node3D` con `OmniLight3D` + `SphereMesh` emisivo; color y parpadeo (`AGONIZANTE`) sincronizados en tiempo real con `Herido.estado_salud`; se apaga (`visible=false`) al morir (RF-14). Solo existe en E1 (único herido hoy) | Modelo de foco final (carcasa, no solo esfera emisiva) | S4/S7 | — | En producción |
| PH-010 | Fuente tipográfica UI | Fuente | Menús y HUD | Fuentes `Caveat`/`Lacquer` en `fonts/` (no son máquina de escribir) | Fuente de máquina de escribir / militar para la estética "documento clasificado" | S9 | — | Pendiente |
| PH-011 | Arte de UI (sticky notes, carpeta, mapa neón, fondo neón, grafiti) | Texturas/UI | Menús, mapa, pausa | `ColorRect`/`Panel` planos | Existen `assets/2D/folder` (+ `confidential.png`), `sticky_note`, `neon_background`. Falta: grafitis e integración final en menús | S9 | — | En producción |
| PH-012 | Música (3 partituras) | Audio | Autoload `GestorAudio` (`scripts/Autoloads/gestor_audio.gd`) | **[S8]** Máquina de estados completa (`MENU`/`COMBATE`/`CRITICO`) con `AudioStreamPlayer` único; cambia sola con `jugador.gd` (combate al iniciar misión, crítico bajo `umbral_salud_critica`) y `menu_principal.gd` (menú). Sin streams asignados: `cambiar_estado()` no suena nada, solo loggea, sin errores | Música original del equipo por estado, asignada a los 3 `@export AudioStream` de `GestorAudio` | S8 | — | En producción |
| PH-013 | SFX (disparos, pasos, ambiente, kit, voz, confirmación) | Audio | Global. `AudioStreamPlayer3D` cableados y disparados en código: `Herido/SonidoGemido`+`SonidoRescate` (S4), `KitMedico/SonidoFeedback` (S3), **[S8]** `EnemigoBase/SonidoPasos` (loop en AVANCE, RF-33), `EnemigoArma/SonidoDisparo`, `EnemigoCuchillo/SonidoEmbestida`, `Pistola/SonidoDisparo`+`SonidoRecarga`, `Escenario_E1_Calle/SonidoAmbiente` y `Escenario_E2_Edificio/SonidoAmbiente` (`autoplay=true`, cubre genéricamente "ambiente urbano"/"explosiones lejanas" hasta que S11 agregue un disparador aleatorio dedicado) | SFX de librerías libres de derechos | S8 | — | En producción |
| PH-014 | Mapa 2D de muñeca (arte) | UI/textura | `views/MapaMuneca.tscn`, instanciado en `Jugador.tscn` bajo `ManoIzquierda` | **[S2/S4]** Mecánica completa y funcional (RF-06..RF-10): `QuadMesh` azul eléctrico como fondo, icono esfera para el jugador (centro fijo), iconos dinámicos por herido (`SphereMesh`, color semafórico real desde S4: verde/amarillo/rojo/gris-oculto al morir), 2 iconos fijos `E1`/`E2` con `Label3D` para saltar de escenario. Activación al levantar la mano izq. por encima de un umbral de altura; selección por proximidad de la mano derecha + gatillo | Existe `assets/2D/2D_neon_map` (`map.blend` + `final.png`). Falta: reemplazar `Fondo`/iconos primitivos por el arte holográfico final e iconos de herido definitivos, sin tocar `mapa_muneca.gd` | S2/S9 | — | En producción |
| PH-015 | Menú de opciones — controles interactivos de Audio/Gráficos/Controles | UI | `main_menu.tscn` → `folder/Contenedor_Opciones` | **[S9]** Solo existe el botón `btn_volver` + un `Sprite3D` decorativo (`Opciones_extra.png`, ya provisto por arte). Ningún control de Audio/Gráficos/Controles es interactivo todavía; `GestorOpciones` (nuevo autoload) tiene `volumen_master` listo para conectar a un control real de Audio | Botones/sliders 3D reales dentro de `Contenedor_Opciones`, con la misma convención Area3D+Tween que `btn_play`/`btn_options`/`btn_exit`. No se agregaron a mano por ser una escena con arte y animaciones ya afinadas por el equipo — alto riesgo de desalinear visualmente sin poder previsualizar en el editor | S9 | — | Pendiente |
| PH-016 | Menú de pausa — panel con fondo desenfocado real | UI/Shader | `views/MenuPausa.tscn` → nodo `Fondo` | **[S9]** Panel oscuro semitransparente sólido (`QuadMesh` + `StandardMaterial3D`), sin blur real | Shader de blur (`BackBufferCopy` + material con `SCREEN_TEXTURE`) sobre el fondo del panel, según UI_ART_Design.md §5.4 ("fondo desenfocado") | S9/S11 | — | Pendiente |
| PH-017 | Pantalla de estado inicial — arte de mesa y documentos | Escenario + UI | `views/EstadoInicial.tscn` | **[S9]** `BoxMesh` beige como "documento" único con `Label3D` de briefing genérico; environment oscuro simple | Mesa, varios documentos con logo/mapa de la misión, iluminación de escena narrativa (UI_ART_Design.md §5.3) | S9 | — | Pendiente |

## 2. Decisiones a confirmar vinculadas (ver `Contexto.md §8`)
- **D1** duración de misión (10 vs 20 min) → parametrizada, confirmar valor.
- **D3** paleta cromática corregida (verde/gris con HEX erróneos) → confirmar con arte.

## 3. Cómo actualizar este archivo (recordatorio para Claude Code)
1. Al introducir un placeholder nuevo, agrega una fila `PH-###` correlativa.
2. Al integrar un asset final, cambia el `Estado` a `Resuelto` (no borres la fila).
3. Refleja el sprint en que se detectó/resolvió.
4. Deja los `Responsable` para que el equipo complete quién produce cada asset.
