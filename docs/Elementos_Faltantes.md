# Elementos Faltantes / Placeholders — SoldierCarer

**Proyecto:** SoldierCarer · Grupo 6
**Documento:** Registro vivo de assets/elementos faltantes y sus placeholders
**Versión:** 1.0 (inicial)

> **Documento vivo.** Claude Code lo **actualiza al cierre de cada sprint** (y al introducir cualquier placeholder nuevo), según la política de `Contexto.md §6-§7`. Las entradas resueltas se marcan como `Resuelto`, no se borran (trazabilidad).
>
> Estados: `Pendiente` · `En producción` · `Resuelto`.
>
> **Convención de estados:** `En producción` = el modelo/asset ya existe en `assets/` (`.blend`, textura…) **pero aún no está integrado** en su escena de juego con el swap del placeholder. `Resuelto` = integrado y reemplazando al placeholder en la escena correspondiente.

---

## 1. Registro

> **Nota de reconciliación (2026-07-18):** el modelado va más avanzado que este registro. Los ítems marcados `En producción` **ya tienen `.blend` en `assets/`** pero aún **no están integrados** en su escena de juego (sigue vivo el placeholder). Al integrarlos (swap del placeholder) pasan a `Resuelto`.

| ID | Elemento | Tipo | Dónde se usa | Placeholder actual | Qué se necesita para reemplazar | Sprint | Responsable | Estado |
|---|---|---|---|---|---|---|---|---|
| PH-001 | Manos + uniforme del jugador VR | Modelo 3D + animación | Rig del jugador (`views/Jugador.tscn`) | **[S1→integrado 2026-07-25]** `PH_Mano`/`PH_Manga` reemplazados: `Mano` instancia `assets/3D/entities/medic/HandRig.tscn` (rig con `Skeleton3D`, extraído de `Hands.blend`, mismo rig espejado para ambas manos) y `Manga` instancia `assets/3D/entities/medic/MangaIzquierda.tscn`/`MangaDerecha.tscn` (extraídos de `Medic.blend`, ya texturizados con `MilitaryLeftArm.png`/`MilitaryRightArm.png`). `manos_vr.gd` ajustado para tintar la malla dentro del rig (ya no es un `MeshInstance3D` directo) | **Pendiente de pulido visual**: los `.blend` fuente no vienen en escala real (personaje ~13 unidades de alto en vez de ~1.8 m — probablemente falta "Apply Scale" en Blender); la escala/posición aplicada en `Jugador.tscn` es una primera aproximación a partir del tamaño del placeholder anterior, falta ajustar a ojo en el editor. Sin animaciones de gesto todavía (dedos no se doblan, solo tiñe color) | S1 | MatSosa1 (arte) / pulido visual pendiente | Resuelto (pulido visual pendiente) |
| PH-002 | Modelo de soldado herido | Modelo 3D + audio | `SoldadoHerido.tscn` | **[S4→integrado 2026-07-25]** Cápsula placeholder reemplazada: `Cuerpo` ahora tiene 7 `MeshInstance3D` planos (Head/LeftArm/RightArm/Pants/Shirt/LeftShoe/RightShoe) extraídos de `Patient.blend`, ya texturizados (`Chest1.png`/`MilitaryLeftArm1.png`/`MilitaryRightArm1.png`). Mismo `Transform3D` correctivo (permutación de ejes + escala ~0.1) aplicado a las 7 piezas para mantenerlas alineadas entre sí. `PuntosDeHerida` y `herido.gd` sin tocar (no hacía falta: los 7 nodos siguen siendo hijos directos de `Cuerpo`, que es lo que `_tenir_cuerpo()`/`_ready()` esperan) | `PuntosDeHerida` reubicado (2026-07-25) recalculando el centro real de cada malla del cuerpo ya con el transform correctivo aplicado (antes quedaban con las coordenadas viejas de la cápsula placeholder, muy lejos del cuerpo nuevo — reportado en juego como heridas "flotando" fuera del cuerpo) | **Pendiente de pulido visual**: transform correctivo del cuerpo sigue siendo una primera aproximación (mismo problema de escala que PH-001 — el .blend fuente no viene en unidades reales); los puntos de herida son una aproximación a partir del centro de cada malla, no de la superficie exacta. Sin posturas de dolor ni set de voz específico (usa `SonidoGemido`/`SonidoRescate` genéricos ya cableados desde S4) | S4 | MatSosa1 (arte) / pulido visual pendiente | Resuelto (pulido visual pendiente) |
| PH-003 | Modelo Opositor con pistola | Modelo 3D + animación | `views/EnemigoArma.tscn` | **[S5→integrado 2026-07-26]** Cápsula naranja reemplazada por las 7 piezas de `Enemy.blend` (mismo rig que `Patient.blend`/`Medic.blend`) de pie, escala real (~1.8m) sin rotación (el rig ya viene Y-arriba). `pistol.blend` agregado como nodo `Arma`, hijo del enemigo, en una posición aproximada de mano (no hay hueso de mano localizado con precisión) | **Pendiente de pulido visual**: posición/rotación del arma en la mano es una estimación a ojo, falta ajustar en el editor. Sin animación de disparo (el rig no trae `AnimationPlayer` en el `.blend`) | S5 | MatSosa1 (arte) / pulido visual pendiente | Resuelto (pulido visual pendiente) |
| PH-004 | Modelo Opositor con navaja | Modelo 3D + animación | `views/EnemigoCuchillo.tscn` | **[S5→integrado 2026-07-26]** Mismo swap que PH-003 (`Enemy.blend` de pie) + `knife.blend` (`Handle`/`Heel`/`Knife`) como nodo `Arma` en posición aproximada de mano | **Pendiente de pulido visual**: igual que PH-003, posición del arma a ojo. Sin animación de embestida | S5 | MatSosa1 (arte) / pulido visual pendiente | Resuelto (pulido visual pendiente) |
| PH-005 | Ítems del kit médico | Modelos 3D | `views/KitMedico.tscn`, instanciado en `Jugador.tscn` (anclado a la cadera, "mochila") | **[S3→integrado 2026-07-25]** `BoxMesh` de cada `Icono` reemplazado por el modelo real: Vendas→`bandage.blend`, Morfina→`needle.blend` (jeringa), Alcohol→`alcohol.blend`, Suturas→`skin_stapler.blend` (grapadora de piel), Analgésicos→`pills/pill.blend`. Escala subida ~1.8x sobre el cálculo inicial por tamaño real (reportado "muy chico y gris" al probarlo) + tinte de color horneado directo en cada malla extraída (blanco vendas, azul morfina, celeste alcohol, gris metal suturas, naranja analgésicos — mismos colores que el placeholder, RF de identificación rápida) para que se distingan a simple vista sobre los materiales grises/sin textura que traen los `.blend`. Mallas planas sin instanciar el `.blend` original (mismo enfoque que la pistola/manos) — un primer intento con `surface_material_override` sobre la instancia directa generó nodos "clash" (choque de nombre con los hijos internos del `.blend`, Godot los duplicaba y renombraba al cargar); se descartó ese enfoque. De paso se limpió un override huérfano en `Mision.tscn` que apuntaba al viejo `PH_Manga` (renombrado a `Manga` en el swap de manos, PH-001) y causaba un warning de carga. Ningún script de gesto tocado (ninguno leía `Icono` por nombre, solo la posición del nodo `Item…` para proximidad) | **Pendiente de pulido visual**: falta confirmar en el editor que cada ítem se ve proporcionado y bien orientado en la mano/mochila. `kit.blend` (el maletín en sí) sigue sin usarse como mochila del jugador — no hay un nodo "mochila" visible en `Jugador.tscn`, solo los íconos flotantes. (Sí se usó `kit.blend` como prop decorativo suelto en el puesto de mando, ver PH-020 — pendiente evaluar si conviene reusarlo también como mochila real, cambiaría `Jugador.tscn`) | S3 | MatSosa1 (arte) / pulido visual pendiente | Resuelto (pulido visual pendiente) |
| PH-006 | Pistola del jugador | Modelo 3D | `views/Jugador.tscn` → `XROrigin3D/ManoDerecha/Pistola/Modelo` | **[S5→integrado 2026-07-25]** `PH_Pistola` reemplazado por `Modelo`, instancia de `assets/3D/weapons/pistol/PistolaModelo.tscn` (malla extraída de `pistol.blend`, sin datos de hueso — sin animación en el .blend fuente). Rotación 90° en Y + escala ~0.022 aplicada para alinear el cañón con el eje -Z local (misma dirección que usa `RayCast3D` para disparar) y llevarlo a tamaño real (~18cm, igual que medía el `BoxMesh` anterior). `pistola.gd` solo cambió `$PH_Pistola` → `$Modelo` (mismo tipo `Node3D`, resto de la mecánica intacta) | **Pendiente de pulido visual**: transform es una primera aproximación a ojo de planos/AABB, falta confirmar en el editor que el cañón apunta bien y el agarre queda natural en la mano. Sigue faltando la funda visible en la cadera (no existía en el placeholder tampoco) | S5 | MatSosa1 (arte) / pulido visual pendiente | Resuelto (pulido visual pendiente) |
| PH-007 | Escenario E1 (Calle) | Escenario + props | `views/Mision.tscn` → `Escenario_E1_Calle/Ambientacion` | **[S7→integrado 2026-07-26]** Swap pieza por pieza (no la escena `Scene1.blend` completa — es un city block denso de 119 nodos, mucho más grande que el corredor de 40x40 jugable; decisión: piezas sueltas manteniendo el layout/colisión actual): 2 vehículos → `destroyed_car/CarroModelo.tscn`, 3 árboles → `tree/ArbolModelo.tscn`, 4 escombros → `glass_shards/EscombroModelo.tscn`. Transform = igual posición/rotación que el placeholder, solo con una escala correctiva calculada por AABB real de cada asset. Colisión de vehículos sin tocar (siguen con `BoxShape3D_vehiculo`) | `EdificioLateralIzq/Der` sumados (2026-07-26): `Building_001` de `Scene1.blend` (edificios 001-004 son la misma malla reutilizada), escala correctiva por altura. Colisión sin tocar (siguen con `BoxShape3D_edificio`) | **Densidad ambiental sumada (2026-07-26)**: +6 edificios de fondo (`EdificioFondoNO/SO/NE/SE`, `EdificioExtra1-6`), +22 escombros extra, +6 árboles extra (`Arbol6-11`), +3 autos extra (`VehiculoAbandonado3-5`) — mismos assets ya integrados, reusados con posición aleatoria (rejection sampling evitando spawns/vehículos/punto de despliegue). Piso con textura real `PisoAgrietado.jpg` (antes color plano), `uv1_scale` para tiling. **`Grafiti` integrado (2026-07-26)**: imagen real (`assets/Fondos/Grafiti.jpg`, subida por el encargado) aplicada como `albedo_texture` sobre el plano placeholder existente (mismo tamaño/posición — ya no es color plano). **Piso/vereda y árboles reemplazados por las piezas reales de `Scene1.blend` (2026-07-26)**: el piso (`PlaneMesh` + `PisoAgrietado.jpg`) era un placeholder aproximado — se reemplazó por `VeredaModelo.tscn` (malla `Sidewalk_001` real extraída de `Scene1.blend`, mismo footprint 38x38, corrigiendo su material blanco sin color a gris concreto); se agregó `Pasto1/2` (`PastoModelo.tscn`, malla `Grass` real, corrigiendo su material blanco a verde) a los costados de la vereda. Los 11 árboles (`Arbol1-11`) usaban `assets/3D/tree/tree.blend` — un asset genérico sin relación con el escenario — y se reemplazaron por `ArbolRealModelo.tscn` (extraído de `Scene1.blend/Tree`: `Log`+4 `Leafs`, colores ya correctos en origen, sin necesitar corrección; AABB combinado real ~5.4m alto confirma que no hace falta escala correctiva, `escala≈1.0`) | S7 | MatSosa1 (arte) / pulido visual pendiente | Resuelto |
| PH-008 | Escenario E2 (Edificio) | Escenario + props | `views/Mision.tscn` → `Escenario_E2_Edificio` | **[S7→integrado 2026-07-26]** Mismo criterio que PH-007 (piezas sueltas sobre el layout actual, no `Scene2.blend` completa — 156 nodos, un interior mucho más grande que la habitación de 20x20 actual): sofá → `sofa/SofaModelo.tscn`, mesa → `table/MesaModelo.tscn`, 2 puertas → `door/PuertaModelo.tscn`, 3 escombros → `glass_shards/EscombroModelo.tscn` (mismo asset que E1). Transform = posición/rotación del placeholder + escala correctiva por AABB | **Mobiliario ampliado (2026-07-26)**: +2 sofás/mesas (`Sofa2/3`, `Mesa2/3`) en la primera pasada de densidad, luego +3 más (`Sofa4/5`, `Mesa4/5`) repartidos para llenar huecos sin chocar con escalera/despliegue/puertas — mismos assets reusados, sin nuevos assets necesarios. Piso con textura real `PisoAgrietado.jpg` aplicada (antes color plano). **Investigado 2026-07-26 (a pedido del encargado, dudaba si el escenario era el real de Mateo)**: se inspeccionó `Scene2.blend` completo — **no es una escena de interior**. Contiene el mismo tipo de contenido que `Scene1.blend` (los mismos `Building_001-004` reutilizados, `Sidewalk`/veredas, puertas, ventanas rotas, vidrios) — es otro bloque de ciudad/exterior, no paredes/piso/techo de un interior. No existe ningún asset de "arquitectura de interior" en el proyecto: las cajas de paredes/techo NO tienen un asset real esperando swap, es un vacío genuino de arte que haría falta encargar aparte si se quiere resolver (no es algo que se pueda arreglar extrayendo más piezas de `Scene2.blend`). **Sin swap todavía**: paredes/techo (siguen como cajas planas), escalera (4 escalones decorativos). Pendiente de pulido visual igual que PH-007 | S7 | MatSosa1 (arte) / pulido visual pendiente | Resuelto (pulido visual pendiente; paredes/techo/escalera sin asset real disponible — no es un placeholder temporal, es un vacío de arte) |
| PH-009 | Focos de emergencia | Modelo + luz | `views/Mision.tscn` → `Escenario_E1_Calle/Herido/Foco` | **[S4]** `Node3D` con `OmniLight3D` + `SphereMesh` emisivo; color y parpadeo (`AGONIZANTE`) sincronizados en tiempo real con `Herido.estado_salud`; se apaga (`visible=false`) al morir (RF-14). Solo existe en E1 (único herido hoy) | Modelo de foco final (carcasa, no solo esfera emisiva) | S4/S7 | — | En producción |
| PH-010 | Fuente tipográfica UI | Fuente | Menús y HUD | Fuentes `Caveat`/`Lacquer` en `fonts/` (no son máquina de escribir) | Fuente de máquina de escribir / militar para la estética "documento clasificado" | S9 | — | Pendiente |
| PH-011 | Arte de UI (sticky notes, carpeta, mapa neón, fondo neón, grafiti) | Texturas/UI | Menús, mapa, pausa | `folder.blend` ya integrado desde antes (`main_menu.tscn` → `Contenedor_Opciones`). **[Integrado 2026-07-26]** `neon_background/final.png` aplicado como `albedo_texture` en el panel `Fondo` de `MenuPausa.tscn` y `ResultadosMision.tscn` (antes color plano); `sticky_note.blend` agregado como prop decorativo en `EstadoInicial.tscn` (ver PH-017). Mapa neón (`2D_neon_map`) ya integrado en `MapaMuneca.tscn` (PH-014) y en el puesto de mando (`PuestoDeMando/PanelMapa/map`, preexistente) | No se encontró ningún asset de "grafiti de menú" distinto del graffiti 3D de calle (PH-007, ya resuelto) — si se refiere a otra cosa, aclarar con el encargado | S9 | — | Resuelto |
| PH-012 | Música (3 partituras) | Audio | Autoload `GestorAudio` (`scripts/Autoloads/gestor_audio.gd`) | **[S8]** Máquina de estados completa (`MENU`/`COMBATE`/`CRITICO`) con `AudioStreamPlayer` único; cambia sola con `jugador.gd` (combate al iniciar misión, crítico bajo `umbral_salud_critica`) y `menu_principal.gd` (menú). Sin streams asignados: `cambiar_estado()` no suena nada, solo loggea, sin errores | Música original del equipo por estado, asignada a los 3 `@export AudioStream` de `GestorAudio` | S8 | — | En producción |
| PH-013 | SFX (disparos, pasos, ambiente, kit, voz, confirmación) | Audio | Global. `AudioStreamPlayer3D` cableados y disparados en código: `Herido/SonidoGemido`+`SonidoRescate` (S4), `KitMedico/SonidoFeedback` (S3), **[S8]** `EnemigoBase/SonidoPasos` (loop en AVANCE, RF-33), `EnemigoArma/SonidoDisparo`, `EnemigoCuchillo/SonidoEmbestida`, `Pistola/SonidoDisparo`+`SonidoRecarga`, `Escenario_E1_Calle/SonidoAmbiente` y `Escenario_E2_Edificio/SonidoAmbiente` (`autoplay=true`, cubre genéricamente "ambiente urbano"/"explosiones lejanas" hasta que S11 agregue un disparador aleatorio dedicado) | SFX de librerías libres de derechos | S8 | — | En producción |
| PH-014 | Mapa 2D de muñeca (arte) | UI/textura | `views/MapaMuneca.tscn`, instanciado en `Jugador.tscn` bajo `ManoIzquierda` | **[S2/S4→fondo integrado 2026-07-26]** Mecánica completa y funcional (RF-06..RF-10): icono esfera para el jugador (centro fijo), iconos dinámicos por herido (`SphereMesh`, color semafórico real desde S4: verde/amarillo/rojo/gris-oculto al morir), 2 iconos fijos `E1`/`E2` con `Label3D` para saltar de escenario. Activación al levantar la mano izq. por encima de un umbral de altura; selección por proximidad de la mano derecha + gatillo. `Fondo` (`QuadMesh`) ahora usa `albedo_texture = assets/2D/2D_neon_map/final.png` (render top-down de la ciudad, cian/negro) en vez de color plano; `albedo_color` a blanco con alpha 0.85 para que se lea la textura, `emission` cian se mantiene a baja intensidad como tinte holográfico encima. `ref.jpg`/`ref2.jpg`/`final_ref.png` de esa carpeta son bocetos de referencia del artista, no assets de juego — no se usaron. `mapa_muneca.gd` sin cambios de lógica (solo el comentario de placeholder) | **Sin swap todavía**: iconos (jugador/heridos/E1/E2) siguen siendo primitivas (`SphereMesh`/`BoxMesh`) — no existe asset propio de íconos en `2D_neon_map`, haría falta encargarlo aparte | S2/S9 | — | Resuelto (pulido visual pendiente, íconos aún placeholder) |
| PH-015 | Menú de opciones — controles interactivos de Audio/Gráficos/Controles | UI | `main_menu.tscn` → `folder/Contenedor_Opciones`; `views/MenuPausa.tscn` | **[S9→Audio integrado 2026-07-28]** `GestorOpciones` suma `volumen_musica`/`volumen_sfx` (además de `volumen_master`) con persistencia en `user://opciones.cfg`; `default_bus_layout.tres` agrega buses `Musica`/`SFX` (ya no todo cuelga de `Master`) con volumen por defecto bajado (-8dB/-6dB) para equilibrar la mezcla (algunos SFX se reportaban "demasiado fuertes"). **[S11→placeholder retirado 2026-07-28]** En el menú principal, 3 `Area3D` (`ControlVolumenMaster/Musica/SFX`, `control_volumen.gd`) dentro de `Contenedor_Opciones` ciclan cada volumen 10% por clic (mismo patrón que `btn_volver`). Se **eliminó** el `Sprite3D` `Opciones_extra` (dibujaba la lista `AUDIO/GRAFICOS/CONTROLES/ACCESIBILIDAD` sin nada detrás) y las 3 filas se reubicaron sobre la hoja bajo un título `AUDIO`: antes se apilaban en el eje Y, que es la **normal** del papel, o sea una detrás de otra y fuera de la hoja (la hoja se recorre en +X y -Z). En `MenuPausa` se agregaron `IconoVolumenMaster/Musica/SFX` a la lista de íconos seleccionables (proximidad+gatillo VR / rueda-Tab escritorio), reordenando los 6 íconos para que entren en el panel | Gráficos/Controles siguen sin sistema que configurar (ver nota original). Posiciones ya **verificadas por medición** (proyección a pantalla de cada etiqueta contra el AABB de `folder/Paper`), no a ojo | S9/S11 | — | Resuelto (Audio); Gráficos/Controles pendiente |
| PH-016 | Menú de pausa — panel con fondo desenfocado real | UI/Shader | `views/MenuPausa.tscn` → nodo `Fondo` | **[S9]** Panel semitransparente. **[Integrado 2026-07-26]** `neon_background/final.png` como `albedo_texture` (antes color plano sólido) — mejora visual, pero sigue sin ser un blur real del fondo del juego. **[UX]** Ancla al mundo a distancia de brazo al abrirse; iconos con color semántico y hover | Shader de blur (`BackBufferCopy` + material con `SCREEN_TEXTURE`) sobre el fondo del panel, según UI_ART_Design.md §5.4 ("fondo desenfocado") — esto es una feature de shader, no un asset | S9/S11 | — | Pendiente |
| PH-017 | Pantalla de estado inicial — arte de mesa y documentos | Escenario + UI | `views/EstadoInicial.tscn` | **[S9]** `BoxMesh` beige como "documento" único con `Label3D` de briefing genérico; environment oscuro simple. **[UX]** Briefing con efecto máquina de escribir + contador visible del auto-avance; `SonidoTecleo` cableado sin stream. **[Integrado 2026-07-26]** `sticky_note.blend` agregado como prop decorativo (`NotaAdhesiva`) apoyado sobre el mismo plano del escritorio, junto al documento principal. `Pared_Con_Puerta.png` (backdrop ilustrado: pared agrietada, puerta, cajas médicas, camilla — encontrado sin usar en `assets/Fondos/`) agregado como `Sprite3D` (`FondoNarrativo`) detrás de la escena, mismo patrón que `Pared_Con_Agujero` en `main_menu.tscn` | Sigue sin existir un asset de mesa — la "mesa" sigue siendo implícita (no hay geometría propia). Documento principal sigue siendo `BoxMesh` sin logo/mapa; clip de tic de máquina de escribir | S9 | — | Pendiente (mejora sustancial de ambientación, pero falta la mesa) |
| PH-018 | Vignette radial para daño y fundido | Textura/Shader | `views/Jugador.tscn` → `XROrigin3D/Camera3D/VignetteDanio` y `Desvanecido3D` | **[UX]** Quads 3D frente a la cámara con tinte **uniforme** (rojo para daño, negro para el fundido de teletransporte) — necesarios porque los `CanvasLayer` 2D no se renderizan en el headset | Textura de gradiente radial (u shader) para que el daño oscurezca solo los bordes de la visión (vignette real, más cómodo en VR que el flash uniforme) | UX | — | Pendiente |
| PH-019 | Pantalla de resultados — arte de fondo | UI | `views/ResultadosMision.tscn` → nodo `Fondo` | **[S10→integrado 2026-07-26]** `neon_background/final.png` aplicado como `albedo_texture` del panel `Fondo` (antes color plano sólido, mismo estilo que `MenuPausa`) | **Pendiente de pulido visual**: es el mismo arte genérico reutilizado en `MenuPausa`/mapa de muñeca, no un fondo específico para la pantalla de resultados — confirmar con arte si se quiere algo distintivo | S10/S11 | — | Resuelto (pulido visual pendiente) |
| PH-020 | Puesto de mando + mapa de despliegue de la ciudad | Escenario + UI | `views/Mision.tscn` → `PuestoDeMando` (fase DESPLIEGUE, `mapa_despliegue.gd`) | **[Flujo]** Mesa, radio y sacos con primitivas; iconos esfera E1/E2 sobre el panel (color semafórico real del herido de cada escenario, clic de mouse o proximidad+gatillo VR). El mapa 3D holográfico (`assets/2D/2D_neon_map/map.blend`) ya estaba integrado en `PanelMapa/map` de antes (no es un `QuadMesh` plano). **[Integrado 2026-07-26]** `PisoPuesto` ahora usa `PisoAgrietado.jpg` (antes color plano); `MaletinMedico` agregado sobre la mesa (`kit.blend`, hasta ahora sin usar — es el maletín que le faltaba a PH-005) | Carpa/mesa de mando con arte final (sigue sin existir ese asset); iconos definitivos de punto de despliegue. Swap sin tocar `mapa_despliegue.gd` | Flujo | — | Pendiente (mesa/carpa final sigue sin asset) |
| PH-021 | Heridas visuales sobre el cuerpo del herido | Modelo/decal + audio | `views/SoldadoHerido.tscn` → `PuntosDeHerida` (generadas por `generador_heridas.gd`, visual en `herida.gd`) | **[Flujo]** Marca `SphereMesh` emisiva por código: color por tipo (laceración/hemorragia/quemadura), tamaño por severidad, pulso en hemorragias, `Label3D` con tipo + siguiente ítem + % del gesto; cuerpo multiparte tendido (torso/cabeza/brazos/piernas con primitivas, reemplaza la cápsula única de PH-002) | Decals/texturas de herida reales sobre `entities/model.blend`, posturas de dolor, sets de voz de dolor por intensidad. Mantener los nombres de los `Marker3D` de `PuntosDeHerida` al hacer el swap | Flujo | — | Pendiente |
| PH-022 | Barra de vitalidad del herido (medidor RF-12/RF-13) | UI/HUD 3D | `scripts/Herido/herido.gd` → `_construir_barra_vitalidad()` (flota sobre el cuerpo, junto a `EtiquetaEstado`) | **[S11]** Dos `QuadMesh` billboard generados por código (fondo oscuro + relleno del color semafórico), sin textura; el relleno se redimensiona por `center_offset` en vez de escalarse, para que crezca desde el borde izquierdo desde cualquier ángulo | Arte de medidor médico diegético (marco/regleta con la estética 2038 de `UI_ART_Design.md`) que sustituya los dos quads planos; la jerarquía y `_actualizar_barra()` ya quedan listas para el swap | S11 | — | Pendiente |

## 2. Decisiones a confirmar vinculadas (ver `Contexto.md §8`)
- **D1** duración de misión (10 vs 20 min) → parametrizada, confirmar valor.
- **D3** paleta cromática corregida (verde/gris con HEX erróneos) → confirmar con arte.

## 3. Cómo actualizar este archivo (recordatorio para Claude Code)
1. Al introducir un placeholder nuevo, agrega una fila `PH-###` correlativa.
2. Al integrar un asset final, cambia el `Estado` a `Resuelto` (no borres la fila).
3. Refleja el sprint en que se detectó/resolvió.
4. Deja los `Responsable` para que el equipo complete quién produce cada asset.
